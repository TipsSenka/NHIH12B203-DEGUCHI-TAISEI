{
  "name": "backend",
  "version": "1.0.0",
  "private": true,
  "description": "架空データを返す学科紹介API（Cloudflare Workers）",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "whoami": "wrangler whoami"
  },
  "devDependencies": {
    "wrangler": "^4.130.0"
  }
}
